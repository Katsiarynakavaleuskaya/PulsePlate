# 📊 Байесовский метод в PulsePlate

## Найдено в закрытом PR #235

Этот документ описывает байесовскую систему анализа тестов, найденную в **закрытом Pull Request #235** ("Feature/cd secrets transport").

---

## 🎯 Что это?

**Байесовская система** — это умная система анализа тестов, которая использует **теорему Байеса** для:

- 🔍 **Предсказания вероятности** падения тестов
- 🩺 **Диагностики причин** падающих тестов
- ⚡ **Оптимизации порядка** выполнения тестов  
- 📈 **Анализа корреляций** между ошибками

### Теорема Байеса

```
P(причина|симптомы) = P(симптомы|причина) * P(причина) / P(симптомы)
```

где:
- **P(причина|симптомы)** — вероятность причины при наличии симптомов
- **P(симптомы|причина)** — вероятность симптомов при данной причине
- **P(причина)** — априорная вероятность причины
- **P(симптомы)** — общая вероятность симптомов

---

## 📁 Файлы в PR #235

### 🏗️ Основные модули (core/)

1. **`core/bayesian_test_analyzer.py`** (771 строка)
   - Главный байесовский анализатор
   - Классы: `BayesianTestAnalyzer`, `TestExecution`, `BayesianDiagnosis`
   - Использует теорему Байеса для диагностики

2. **`core/comprehensive_bayesian_analyzer.py`** (510 строк)
   - Комплексный анализатор всех аспектов
   - Технические + питательные + бизнес аспекты
   - Класс: `ComprehensiveBayesianAnalyzer`

3. **`core/integrated_bayesian_analyzer.py`**
   - Интегрированный анализатор
   - Связывает все компоненты

4. **`core/nutrition_bayesian_analyzer.py`**
   - Байесовский анализ питания
   - Проверка безопасности пищевых рекомендаций

5. **`core/business_bayesian_analyzer.py`**
   - Анализ бизнес-логики
   - Монетизация и рост клиентской базы

### 🛠️ Скрипты (scripts/)

6. **`scripts/analyze_failed_tests_bayesian.py`** — анализ падающих тестов
7. **`scripts/bayesian_quality_report.py`** — отчеты о качестве
8. **`scripts/bayesian-pre-commit.py`** — pre-commit хук
9. **`scripts/bayesian-pre-commit-fast.py`** — быстрая версия хука
10. **`scripts/bayesian-pre-commit-hook.py`** — Git хук
11. **`scripts/run_tests_bayesian.py`** — запуск тестов с анализом
12. **`scripts/bayesian_debug_helper.py`** — помощник отладки

### 🔌 Плагин

13. **`pytest_bayesian_plugin.py`** — плагин pytest для байесовского анализа

### ✅ Тесты (tests/)

14. **`tests/test_bayesian_analyzer.py`** — тесты анализатора
15. **`tests/test_comprehensive_bayesian_analyzer.py`** — тесты комплексного анализатора

---

## 🚀 Примеры использования

### 1️⃣ Базовое использование

```python
from core.bayesian_test_analyzer import (
    BayesianTestAnalyzer,
    TestExecution,
    TestCategory,
    TestResult,
    ErrorType
)

# Создание анализатора
analyzer = BayesianTestAnalyzer()

# Запись выполнения теста
execution = TestExecution(
    test_name="test_api_endpoint",
    category=TestCategory.INTEGRATION,
    result=TestResult.FAILED,
    error_type=ErrorType.ASSERTION_ERROR,
    error_message="Expected 200, got 404",
    execution_time=0.5,
    coverage_percentage=85.0,
    file_path="tests/test_api.py"
)

analyzer.record_test_execution(execution)

# Диагностика ошибки
diagnosis = analyzer.diagnose_test_failure(
    test_name="test_api_endpoint",
    error_message="Expected 200, got 404",
    context={"has_mocks": True}
)

print(f"Наиболее вероятная причина: {diagnosis.most_likely_cause}")
print(f"Вероятность: {diagnosis.probability:.2%}")
print(f"Уверенность: {diagnosis.confidence:.2%}")
```

### 2️⃣ Предсказание падений

```python
# Предсказать вероятность падения теста
probability = analyzer.predict_test_failure_probability(
    test_name="test_database_connection",
    context={
        "recent_changes": True,
        "complex_dependencies": True,
        "async_test": True
    }
)

print(f"Вероятность падения: {probability:.2%}")
```

### 3️⃣ Оптимизация порядка тестов

```python
# Список тестов
tests = [
    "test_simple_function",
    "test_api_endpoint",
    "test_database_query",
    "test_complex_integration"
]

# Оптимизировать порядок (сначала те, что вероятнее упадут)
optimized_tests = analyzer.optimize_test_order(tests)

for test in optimized_tests:
    prob = analyzer.predict_test_failure_probability(test)
    print(f"{test}: {prob:.2%} вероятность падения")
```

### 4️⃣ Оценка здоровья тестов

```python
# Получить оценку здоровья теста
health_score = analyzer.get_test_health_score("test_api_endpoint")

if health_score > 0.8:
    print(f"✅ Тест здоров: {health_score:.2%}")
elif health_score > 0.5:
    print(f"⚠️ Тест требует внимания: {health_score:.2%}")
else:
    print(f"❌ Тест в плохом состоянии: {health_score:.2%}")
```

### 5️⃣ Комплексный анализ

```python
from core.comprehensive_bayesian_analyzer import ComprehensiveBayesianAnalyzer

comprehensive = ComprehensiveBayesianAnalyzer()

test_code = '''
def test_nutrition_calculation():
    result = calculate_nutrition(food_item)
    assert result.calories > 0
'''

result = comprehensive.analyze_comprehensively(
    test_code=test_code,
    test_name="test_nutrition_calculation",
    file_path="tests/test_nutrition.py"
)

print(f"Технический балл: {result.technical_score:.2%}")
print(f"Балл питания: {result.nutrition_score:.2%}")
print(f"Бизнес-балл: {result.business_score:.2%}")
print(f"Общий балл: {result.overall_score:.2%}")
```

---

## 🎓 Как это работает

### Пример диагностики

**Тест падает с ошибкой "AttributeError"**

#### 1️⃣ Априорная вероятность P(причина)

Основана на исторических данных:
- `AttributeError` случается в **15%** случаев
- `TypeError` случается в **20%** случаев
- `AssertionError` случается в **25%** случаев

#### 2️⃣ Правдоподобие P(симптомы|причина)

Вероятность увидеть определенные симптомы:
- Если причина `AttributeError`, то **90%** вероятность увидеть "has no attribute"
- Если причина `TypeError`, то только **10%** вероятность

#### 3️⃣ Апостериорная вероятность P(причина|симптомы)

После применения теоремы Байеса:

```
P(AttributeError|"has no attribute") = 
    P("has no attribute"|AttributeError) * P(AttributeError) / P("has no attribute")
  = 0.90 * 0.15 / P(evidence)
  ≈ 0.85 (85%)
```

---

## ⚙️ Настройка

### Переменные окружения

```bash
# Включить сохранение истории
export BAYESIAN_PERSIST=1

# Путь к файлу истории
export BAYESIAN_HISTORY_PATH="/tmp/test_execution_history.json"
```

### Параметры конфигурации

```python
half_life_hours = 24 * 7  # Полураспад давности: 1 неделя
alpha = 1.0               # Laplace smoothing
similarity_threshold = 0.3 # Порог схожести симптомов
```

---

## 🔗 Как получить код

### Способ 1: Переключиться на ветку PR #235

```bash
# Fetch the PR
git fetch origin pull/235/head:pr-235

# Checkout the branch
git checkout pr-235

# Теперь файлы доступны
ls core/bayesian_test_analyzer.py
```

### Способ 2: Просмотр на GitHub

Перейдите по ссылке:
👉 **https://github.com/Katsiarynakavaleuskaya/PulsePlate/pull/235**

---

## 📖 Дополнительные ресурсы

1. **`bayesian.py`** — полная документация на английском и русском
2. **PR #235** — оригинальный код со всеми файлами
3. **Теорема Байеса** — https://ru.wikipedia.org/wiki/Теорема_Байеса

---

## 🎯 Практические применения

### В CI/CD
- ⚡ Приоритизация тестов по вероятности падения
- 🔍 Быстрое выявление проблем
- 🤖 Автоматическая диагностика при падениях

### В разработке
- 💡 Рекомендации по исправлению ошибок
- 🔬 Анализ качества кода
- ❤️ Оценка здоровья тестов

### В мониторинге
- 📈 Отслеживание трендов качества
- 🎯 Выявление проблемных областей
- 🔮 Прогнозирование будущих проблем

---

## 🏆 Ключевые преимущества

✅ **Научная основа** — теорема Байеса из теории вероятностей  
✅ **Адаптивность** — учится на исторических данных  
✅ **Количественные оценки** — вероятности, уверенность, баллы  
✅ **Практичность** — конкретные рекомендации по исправлению  

---

## 📝 Заключение

Байесовская система в PulsePlate — это мощный инструмент для автоматизации и улучшения процесса тестирования. Она помогает:

- 🎯 Быстрее находить проблемы
- 💡 Понимать причины ошибок
- ⚡ Оптимизировать процесс тестирования
- 📈 Повышать качество кода

**Для полного кода см. Pull Request #235:**  
https://github.com/Katsiarynakavaleuskaya/PulsePlate/pull/235

---

© 2025 PulsePlate AI Team — All Rights Reserved
