receivers:
  otlp:
    protocols:
      http: {}
      grpc: {}

processors:
  batch: {}
  attributes:
    actions:
      - key: service.environment
        action: upsert
        value: prod

exporters:
  debug:
    verbosity: basic

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [attributes, batch]
      exporters: [debug]
