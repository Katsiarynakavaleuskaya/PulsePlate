-c requirements.txt

# Optional vector runtime profile for RAG embeddings + pgvector search.
# Feature-flagged via FEATURE_RAG_VECTOR; keep opt-in only.
# FastEmbed uses ONNXRuntime for local embeddings.
fastembed==0.8.0
pgvector==0.5.0
