# Legacy nosec TTL allowlist (Phase 2 shrink-to-zero).
# Prefer canonical inline suppressions on each line:
#   # nosec BXXX: <why> (remove-by: YYYY-MM-DD, ref: <issue/pr>)
# Empty allowlist: all suppressions must validate inline (tests/guards/test_nosec_policy_guard.py).
