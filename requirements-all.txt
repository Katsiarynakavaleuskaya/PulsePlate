# Production dependencies (canonical source: requirements.txt)
-r requirements.txt

# Development & Testing tools (use constraints.txt for exact versions)
pytest>=8.3
pytest-cov>=6.0
pytest-asyncio>=1.2
coverage>=7.0
black>=24.0
flake8>=7.0
mypy>=1.10
pre-commit>=3.7
bandit>=1.7
# safety>=3.2  # Temporarily disabled due to typer compatibility issue
ruff>=0.13
sourcery>=2.0
