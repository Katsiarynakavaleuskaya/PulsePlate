# Philosophy Epic V2 PR-4 Closed-Marker Preconditions Packet

## Goal

Define a machine-checkable blocked handoff inventory for future Philosophy
semantic-cache runtime admission while the semantic-cache markers remain closed.

PR-4 is governance/test infrastructure only. It does not add Redis, GPTCache,
embeddings, vector search, providers, clients, DB, OpenAPI, frontend, iOS,
`/insight`, cache I/O, or runtime activation.

## Coordinator Route

- Start gate: `check_preflight.py` and `task_bootstrap.py`
- Lane packet: `artifacts/orchestration/task_packets/2061557f7b17.json`
- Required role order:
  `agent-coordinator -> architecture-specialist -> philosophy-agent -> qa-engineer-agent -> security-auditor -> bug-hunter`
- Mandatory post-open lane:
  `qa-engineer-agent -> bug-hunter -> security-auditor`
- PR mode: ready for review, not draft

## Handoff Sources

PR-4 reads and validates these upstream governance sources:

- PR-2 policy oracle:
  `docs/orchestration/contracts/PHILOSOPHY_SEMANTIC_CACHE_ADMISSION_POLICY.json`
- PR-2 generated oracle fixture:
  `tests/fixtures/orchestration/philosophy_admission_claim_oracle.json`
- PR-3 dry-run report:
  `docs/orchestration/contracts/PHILOSOPHY_ADMISSION_DRY_RUN_REPORT.json`
- Semantic-cache roadmap markers:
  `docs/roadmap/PulsePlate_Semantic_Cache_Gate_and_Plan.md`
- Runtime prerequisite anchors:
  `docs/roadmap/BACKLOG_LEDGER.md`
- External predecessor:
  PR #1789 / `codex/philosophy-alignment-rule-trust-schema`

PR #1789 remains an external predecessor for this lane. PR-4 records whether the
alignment-rule schema is present and minimally valid on the current base, but
file presence alone is not merge-readiness proof.

## Deterministic Contract

PR-4 adds `PHILOSOPHY_GATE_OPEN_PRECONDITIONS_REPORT.json` and its schema. The
report is generated by `scripts/ci/check_philosophy_gate_open_preconditions.py`.

Required invariants:

- semantic-cache roadmap markers stay closed
- `gate_open_allowed=false`
- `runtime_handoff_allowed=false`
- `cache_read_allowed=false`
- `cache_write_allowed=false`
- `serving_allowed=false`
- ledger anchor presence does not verify prerequisite closure
- a later reviewed marker-change PR is still required before runtime work can begin

The report may mark PR-2 and PR-3 governance sources as `source_current` only.
That status means the upstream non-runtime source validates. It does not grant
runtime authority.

## Research And Red-Team Basis

Research inputs are rationale-only:

- W3C PROV-DM: explicit source lineage
  (`https://www.w3.org/TR/prov-dm/`)
- OPA decision-log practice: auditable policy-derived decisions
  (`https://www.openpolicyagent.org/docs/management-decision-logs`)
- NIST AI 600-1: red-team findings as governance evidence
  (`https://nvlpubs.nist.gov/nistpubs/ai/NIST.AI.600-1.pdf`)
- Metamorphic testing: pair current governance controls with blocking runtime
  preconditions (`https://doi.org/10.1109/TSE.2016.2532875`)

No external research source overrides repo contracts, tests, roadmap markers, or
current GitHub truth.

## Premortem Closure

Frame: it is six months from now and this PR failed by letting later work imply
Philosophy semantic-cache serving was permitted.

Required closures:

- **FIXED:** checker and schema enforce closed markers and false runtime/cache
  permissions.
- **FIXED:** report distinguishes ledger anchor presence from prerequisite
  closure proof.
- **FIXED:** PR #1789 predecessor is not accepted from filename-only presence.
- **FIXED:** tests cover marker drift, schema weakening, report drift, and
  predecessor ambiguity.

Any new premortem, security, QA, bug-hunter, CodeRabbit, Cubic, or Codex
Security finding must be closed as `FIXED`, `NOT-A-BUG`, or `DEFERRED` before
mapping or readiness claims.

## Experiment Runner Evidence

Experiment Runner may participate only after coordinator bootstrap and only as
oracle-only governance evidence. If its result materially shapes code,
validation, mapping, review disposition, or readiness, the commit must use:

`Co-authored-by: PulsePlate Experiment Runner <pulseplate@pm.me>`

If it is not used, the PR body records `Not applicable` with the reason.

## Validation

Focused gates:

- `python3 scripts/ci/check_semantic_cache_gate.py --check-philosophy-admission-oracle-drift`
- `python3 scripts/ci/check_philosophy_admission_dry_run.py --check`
- `python3 scripts/ci/check_philosophy_gate_open_preconditions.py --check`
- `python3 scripts/orchestration/check_agent_consistency.py`
- focused pytest for Philosophy policy, dry-run, preconditions, semantic-cache
  gate, and docs phase guards
- `DEV_PYTHON=.venv/bin/python VENV_PYTHON=.venv/bin/python make validate-changed`
- `pre-commit run --all-files`

No full `make verify` is required for this operator-approved narrow governance
lane.
