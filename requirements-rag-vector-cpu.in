-c requirements.txt

# Optional vector runtime profile for RAG embeddings + pgvector search.
# CPU/local profile via FastEmbed's ONNXRuntime backend.
# Keep feature-flagged via FEATURE_RAG_VECTOR and deterministic with fixed pins.
fastembed==0.8.0
pgvector==0.4.2
