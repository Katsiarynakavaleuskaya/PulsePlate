# Constraints file for reproducible dev environments
# Use with: pip install -r requirements-dev.txt -c constraints.txt

# Testing
pytest>=8.4.2,<9
pytest-cov>=7.0.0,<8
pytest-asyncio>=0.23.0,<2
coverage>=7.12.0,<8

# Code quality
black==25.9.0
flake8==7.3.0
mypy==1.18.2
ruff>=0.14.2,<0.15.0  # Narrow range for reproducible builds; upgrade to newer minor versions only after testing new lint rules

# Security
bandit==1.8.6
# safety==3.6.1b0  # Disabled due to typer compatibility

# Dev tools
pre-commit==4.3.0
# sourcery==2.0.0  # Not used locally - using GitHub App instead
asgi-lifespan==2.1.0
diff-cover==9.0.0
