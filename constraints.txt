# Constraints file for flexible dependency ranges
# Use with: pip install -r requirements-dev.txt -c constraints.txt
# Strategy: Use >= for flexibility, allowing pip to resolve compatible versions
# This approach enables automatic security patches and bug fixes while maintaining
# compatibility. For fully reproducible builds, use pip freeze > requirements-lock.txt

# Testing - allow minor/patch updates for bug fixes
pytest>=9.1.1
pytest-cov>=7.0.0
pytest-asyncio>=1.4.0
coverage>=7.15.1  # Must match requirements-dev.txt minimum

# Code quality - allow updates for new features/fixes
# Note: Makefile lint still uses flake8; ruff remains the fast companion guard.
black>=26.5.1
# mypy 1.13.x adds support for typing.ReadOnly (PEP 742).
# Type-parameter defaults are available only when using the legacy TypeVar-like syntax.
mypy>=2.2.0,<3.0.0
ruff>=0.15.21  # Fast local/pre-commit lint companion.

# Security - allow patch updates for vulnerability fixes
bandit>=1.8.6
pip-audit>=2.10.1
setuptools>=83.0.0  # Security floor for CVE-2026-59890

# Dev tools - allow minor updates
pre-commit>=4.6.0
sourcery>=2.0.0
diff-cover>=10.3.0  # Updated to match requirements-dev.txt

# Production runtime packages (mirror requirements.txt minimums)
fastapi>=0.138.1
# Starlette 1.3.1 clears the June 2026 SFTY-20260615 advisory cluster.
# FastAPI 0.138.1 metadata allows Starlette >=0.46.0 with no upper cap.
starlette>=1.3.1
uvicorn>=0.38.0
pydantic>=2.13.4
openai>=2.8.1
httpx>=0.28.1
requests>=2.33.0  # GHSA-gc5v-m9x4-r6x2 security floor
click==8.3.3  # Security floor for unsafe editor invocation; exact lock parity
pillow==12.3.0  # July 2026 Pillow advisory cluster; exact lock parity
python-dotenv>=1.2.1
prometheus-client>=0.23.1
python-multipart>=0.0.31  # SFTY-20260615 multipart parser advisory floor
slowapi>=0.1.9
tenacity>=9.1.2
matplotlib>=3.10.7
reportlab>=4.4.4
sqlalchemy>=2.0.44
aiosqlite>=0.22.1
alembic>=1.17.2
mako>=1.3.12  # GHSA-v92g-xgxw-vvmm / GHSA-2h4p-vjrc-8xpq security floor
# Constraints files cannot include extras; requirement uses psycopg[binary]
psycopg>=3.2.3
email-validator>=2.3.0
cryptography>=50.0.0
# Guidance for agentops compatibility (agentops requires packaging<25).
# Not enforced here to avoid conflicts with the current lock files.
# If you need agentops locally, use an isolated venv or a separate constraints-agentops.txt.
