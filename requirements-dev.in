-c requirements.txt
# Use compatible release (~=) for stable dev tools to prevent breaking changes
# Keep security tools (bandit, pip-audit) with >= for latest vulnerability feeds
pytest~=9.0.3
pytest-cov~=7.1.0
pytest-asyncio~=1.3.0
pytest-xdist~=3.8.0
pytest-picked~=0.5.0
pytest-split~=0.11.0
hypothesis~=6.151.12
faker~=40.13.0  # Test data generation (40.11.1 yanked / unavailable on PyPI)
# coverage and diff-cover: use compatible release for stability
coverage~=7.13.5
diff-cover~=10.2.0
# Code quality tools: use compatible release for stability
flake8~=7.3.0
bandit>=1.8.6  # Security tool - keep >= for vulnerability updates
# safety removed: CVE-2025-14009 (nltk transitive), see docs/security/CVE-2025-14009-nltk.md
pip-audit>=2.9.0  # Security tool - keep >= for vulnerability updates
marshmallow>=4.1.2  # Explicit floor to keep CVE fix independent of transitive deps
cryptography>=46.0.7  # Keep explicit dependency floor for dependency-security guard
requests>=2.33.0  # Security floor for GHSA-gc5v-m9x4-r6x2 across dev tooling surfaces
pillow>=12.2.0  # Security floor for GHSA-whj4-6x5x-4v2j across dev tooling surfaces
python-multipart>=0.0.26  # Security floor for GHSA-mj87-hwqh-73pj across dev tooling surfaces
pre-commit~=4.5.1
pip-tools~=7.5.3
detect-secrets~=1.5.0
mypy~=1.20.0
types-pyyaml~=6.0.12
yamllint~=1.38.0
black~=26.3.1
ruff~=0.15.10
nh3>=0.3.2  # HTML sanitization (required for data_sanitizer security tests)
