-c requirements.txt
# Use compatible release (~=) for stable dev tools to prevent breaking changes
# Keep security tools (bandit, pip-audit) with >= for latest vulnerability feeds
# Starlette TestClient backend for local tests; keep out of runtime/Docker/ci-lite profiles.
# 2.4+ requires a newer idna floor than the current runtime lock.
httpx2>=2.3.0,<2.4.0
pytest~=9.1.1
pytest-cov~=7.1.0
pytest-asyncio~=1.4.0
pytest-xdist~=3.8.0
pytest-picked~=0.5.0
pytest-split~=0.11.0
hypothesis~=6.156.6
faker~=40.28.1  # Test data generation (40.11.1 yanked / unavailable on PyPI)
# coverage and diff-cover: use compatible release for stability
coverage~=7.15.1
diff-cover~=10.3.0
# Code quality tools: use compatible release for stability
flake8~=7.3.0
bandit>=1.8.6  # Security tool - keep >= for vulnerability updates
# safety removed: CVE-2025-14009 (nltk transitive), see docs/security/CVE-2025-14009-nltk.md
pip-audit>=2.10.1  # Security tool - keep >= for vulnerability updates
marshmallow>=4.1.2  # Explicit floor to keep CVE fix independent of transitive deps
cryptography>=48.0.1,<49.0.0  # Keep explicit dependency floor for dependency-security guard
requests>=2.33.0  # Security floor for GHSA-gc5v-m9x4-r6x2 across dev tooling surfaces
click>=8.3.3,<9.0.0  # Security floor for unsafe editor invocation across dev tooling surfaces
pillow>=12.3.0,<13.0.0  # Security floor for the July 2026 Pillow advisory cluster
python-multipart>=0.0.31,<1.0.0  # SFTY-20260615 multipart parser floor across dev tooling surfaces
starlette>=1.3.1,<2.0.0  # SFTY-20260615 Starlette floor across dev tooling surfaces
mako>=1.3.12,<2.0.0  # Security floor for GHSA-v92g-xgxw-vvmm / GHSA-2h4p-vjrc-8xpq across dev tooling surfaces
msgpack>=1.2.1,<2.0.0  # Security floor for GHSA-6v7p-g79w-8964 via CacheControl/pip-audit
pre-commit~=4.6.0
pip-tools~=7.5.3
detect-secrets~=1.5.0
mypy==2.2.0
types-pyyaml~=6.0.12
yamllint~=1.38.0
black~=26.5.1
ruff~=0.15.21
nh3>=0.3.2  # HTML sanitization (required for data_sanitizer security tests)
