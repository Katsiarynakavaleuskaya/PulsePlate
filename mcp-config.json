{
  "$schema": "https://modelcontextprotocol.io/schemas/mcp.json",
  "_comment": "Simplified MCP configuration to reduce memory usage",
  "_warning": "WARNING: This file contains API key placeholders. Replace with actual keys and DO NOT commit to version control!",
  "mcpServers": {
    "pulseplate": {
      "command": "python",
      "args": ["mcp_pulseplate_server.py"],
      "env": {
        "OPENAI_API_KEY": "your_openai_api_key_here",
        "MCP_MAX_MEMORY_MB": "512",
        "MCP_TIMEOUT_SECONDS": "30"
      }
    }
  }
}
