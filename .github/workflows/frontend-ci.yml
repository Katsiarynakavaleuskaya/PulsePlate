name: Frontend CI

on:
  pull_request:
    branches: [main, develop, feat/**, fix/**]
  push:
    branches: [main, develop]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Frontend CI temporarily disabled while reverting PR #154"
