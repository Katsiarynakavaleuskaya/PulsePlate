# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
# CD Workflow - Continuous Deployment
#
# Simplified CD workflow: Build and push Docker images only.
# SSH deployment steps are commented out until staging/production servers are available.
#
# When servers are ready:
# 1. Uncomment SSH deployment steps
# 2. Add secrets to GitHub environments (staging, production)
# 3. Configure SSH_HOST_STAGING, SSH_HOST_PRODUCTION, SSH_KEY, etc.

name: CD

on:
  push:
    branches: [main]
    tags: ['v*']

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: staging
      # Staging domain (subdomain or free domain)
      # Options: staging.yourdomain.com, pulseplate-staging.duckdns.org, or free Freenom domain
      # Set STAGING_DOMAIN secret in GitHub Environment (e.g., "pulseplate-staging.duckdns.org")
      # Note: environment.url cannot use secrets context, using static fallback
      url: https://staging.pulseplate.app
    concurrency:
      group: cd-build-${{ github.ref_name }}
      cancel-in-progress: true
    steps:
      - name: Checkout
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435

      - name: Log in to Container Registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute lowercase image repository
        run: echo "IMAGE_REPO=$(echo \"${GITHUB_REPOSITORY}\" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Build & Push image (staging)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25
        with:
          context: .
          push: true
          tags: ghcr.io/${{ env.IMAGE_REPO }}:staging-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Use dedicated staging target which extends production but allows
          # staging-specific customizations (e.g., debug logging, extended health checks)
          target: staging

      # SSH deployment for staging (commented out until servers are available)
      # - name: Prepare known_hosts (staging)
      #   run: |
      #     mkdir -p ~/.ssh
      #     ssh-keyscan -H ${{ secrets.SSH_HOST_STAGING }} >> ~/.ssh/known_hosts 2>/dev/null
      #     chmod 644 ~/.ssh/known_hosts
      #
      # - name: Deploy to staging over SSH
      #   uses: appleboy/ssh-action@3ca8a7c5359ac6ad91aa47f1946ece1c3b025004
      #   with:
      #     host: ${{ secrets.SSH_HOST_STAGING }}
      #     username: ${{ secrets.SSH_USER }}
      #     key: ${{ secrets.SSH_KEY }}
      #     script: |
      #       set -euo pipefail
      #       export GHCR_USER='${{ github.actor }}'
      #       export GHCR_TOKEN='${{ secrets.GHCR_READ_TOKEN }}'
      #       export STAGING_DOMAIN='${{ secrets.STAGING_DOMAIN }}'
      #       cd /srv/pulseplate-staging
      #       ./deploy.sh '${{ github.sha }}'
      #
      # - name: Healthcheck (staging)
      #   run: |
      #     curl -fsS --max-time 5 "https://${{ secrets.STAGING_DOMAIN }}/health" > /dev/null

  build-production:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment:
      name: production
      # Production domain from secrets (configure in GitHub Environments)
      # For free tier: use Freenom (.tk, .ml, .ga, .cf) or DuckDNS subdomain
      # For budget: Namecheap .xyz ($1-2/year) or Cloudflare Registrar ($1.03/year for .xyz)
      # Set PRODUCTION_DOMAIN secret (e.g., "pulseplate.xyz")
      # Note: environment.url cannot use secrets context, using static fallback
      url: https://pulseplate.app
    concurrency:
      group: cd-build-production-${{ github.ref_name }}
      cancel-in-progress: true
    steps:
      - name: Display production domain info
        run: echo "Deploying to ${{ secrets.PRODUCTION_DOMAIN || 'default production domain' }}"

      - name: Checkout
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435

      - name: Log in to Container Registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute lowercase image repository
        run: echo "IMAGE_REPO=$(echo \"${GITHUB_REPOSITORY}\" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Build & Push image (production)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25
        with:
          context: .
          push: true
          tags: ghcr.io/${{ env.IMAGE_REPO }}:latest,ghcr.io/${{ env.IMAGE_REPO }}:prod-${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          target: production

      # SSH deployment steps (commented out until servers are available)
      # When ready, uncomment and configure SSH_HOST_PRODUCTION, SSH_USER, SSH_KEY secrets
      # - name: Prepare known_hosts
      #   run: |
      #     mkdir -p ~/.ssh
      #     ssh-keyscan -H ${{ secrets.SSH_HOST_PRODUCTION }} >> ~/.ssh/known_hosts 2>/dev/null
      #     chmod 644 ~/.ssh/known_hosts
      #
      # - name: Deploy to production over SSH
      #   uses: appleboy/ssh-action@3ca8a7c5359ac6ad91aa47f1946ece1c3b025004
      #   with:
      #     host: ${{ secrets.SSH_HOST_PRODUCTION }}
      #     username: ${{ secrets.SSH_USER }}
      #     key: ${{ secrets.SSH_KEY }}
      #     script: |
      #       set -euo pipefail
      #       export GHCR_USER='${{ github.actor }}'
      #       export GHCR_TOKEN='${{ secrets.GHCR_READ_TOKEN }}'
      #       export PRODUCTION_DOMAIN='${{ secrets.PRODUCTION_DOMAIN }}'
      #       cd /srv/pulseplate-production
      #       ./deploy.sh '${{ github.sha }}'
      #
      # - name: Post-deploy healthcheck
      #   run: |
      #     URL="https://${{ secrets.PRODUCTION_DOMAIN }}/health"
      #     for i in $(seq 1 10); do
      #       if curl -fsS --max-time 5 "$URL" > /dev/null 2>&1; then
      #         echo "✅ Health check passed"
      #         exit 0
      #       fi
      #       echo "⏳ Waiting for health check ($i/10)..."
      #       sleep 6
      #     done
      #     echo "❌ Health check failed after deployment"
      #     exit 1
