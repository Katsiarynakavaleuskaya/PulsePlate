name: security

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop, feat/**, fix/**]

jobs:
  bandit:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Security scan temporarily disabled while reverting PR #154'
