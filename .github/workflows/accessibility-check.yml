name: Accessibility Tests

on:
  pull_request:
    branches: [main, develop, feat/**, fix/**]
  push:
    branches: [main, develop]

jobs:
  accessibility:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Accessibility workflow temporarily disabled while reverting PR #154"
